#include "../common.h"

// QStyle *QStyleFactory::create(const QString &key)

/*
void * qstylefactory(void)
{
    return new QStyleFactory();
}

QStyle *  qstylefactorycreate(const char * key) 
{
    QString qstring(key);

    return new QStyleFactory(qstring);
}
*/
//